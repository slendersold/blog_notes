[package]
name = "blog-integration-tests"
description = "CLI + server + Postgres; бинарники blog-cli/blog-server — отдельной сборкой (build.rs только проверяет, без вложенного cargo)."
publish = false
version.workspace = true
edition.workspace = true
build = "build.rs"

[lib]
path = "src/lib.rs"

[dependencies]

[dev-dependencies]
rand = "0.8"
reqwest = { workspace = true, features = ["blocking"] }
serde_json.workspace = true
tempfile = "3"
